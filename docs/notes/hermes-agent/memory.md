---
title: Hermes Agent 学习笔记 · 第五章 · 记忆系统
pageClass: notes-doc
outline: [2, 3]
aside: true
---

## 第五章 记忆系统——Agent 怎么"记住事"的

> 🎙️ **老师开讲**：四层楼的第一层，也是最基础的——Agent 得先有记性，后面才能谈别的。你想一个场景：普通 AI 助手像个金鱼，你跟它聊了三天，每次都从零开始。Hermes 不一样——它有两个小本本。一个叫 MEMORY.md，记的是"工作上的事"——"用户用 pnpm""项目部署在 Vercel""团队用 Biome 做格式化"；另一个叫 USER.md，记的是"你这个人"——"说话喜欢简洁""做技术决策时要详细分析"。两个本子加起来才三千多字，故意很小——因为空间越小，Agent 越被迫只记最重要的。那么问题来了：Agent 怎么知道什么时候该记？记完之后怎么不影响 AI 调用的缓存？两个会话同时写会不会打架？下面一个个讲清楚。

### 5.1 要解决什么问题

大多数 Agent 的记忆在会话结束就清零了。下次对话，你得重新告诉它"我用 pnpm""代码用 4 空格缩进"——每次都像新来的同事。

Hermes 要让 Agent **能记住**。但"记住"不是简单把聊天记录存下来，得解决三个问题：

1. **记什么**——不能什么都记，那跟没记一样
2. **怎么存**——得可靠，不能丢
3. **怎么读**——有个很巧妙的工程细节，后面会讲

### 5.2 两个文件搞定一切：MEMORY.md 和 USER.md

Hermes 把记忆分成两个 Markdown 文件：

| 文件 | 容量上限 | 存什么 | 类比 |
|:---|:---|:---|:---|
| `MEMORY.md` | 2,200 字符 | 环境事实、工具怪癖、项目约定 | Agent 的工作笔记本 |
| `USER.md` | 1,375 字符 | 你的偏好、沟通风格、工作习惯 | Agent 对你的人物画像 |

加起来只有 **3,600 字符**——这个限制是**故意的**。空间就那么大，Agent 被迫只记最有价值的东西，不能什么都往里塞。

**对比 GA**：GA 的记忆有 L0-L4 五层（Meta Rules / Insight Index / Global Facts / Task Skills / Session Archive），分得更细。Hermes 更"粗暴"——就两个文件，但配合全文搜索，效果也不错。

**💡 生动例子——这两个文件里到底写了什么**

咱们打开 Agent 的"小本本"，看看用了一个月后里面长什么样：

**MEMORY.md（工作笔记本，2,200 字符上限）：**

```markdown
## 项目约定
- 所有项目用 pnpm，不用 npm 或 yarn
- 代码缩进 4 空格，不是 Tab
- 提交信息用中文，格式"feat: xxx"

## 环境信息
- 操作系统 macOS 15，Apple Silicon
- Node.js v22，Python 3.12
- Vercel CLI 已安装，token 在 ~/.vercel/token

## 工具怪癖
- pnpm 在 monorepo 下需要加 -w 参数
- Vercel 部署 SSL 证书偶尔需要手动续期

## 上次学习
- 2026-06-15: 用户纠正——部署前必须跑一遍单元测试
- 2026-06-18: 用户纠正——端口默认 3000，不是 8080
```

**USER.md（人物画像，1,375 字符上限）：**

```markdown
## 沟通风格
- 喜欢简洁直接的回复，不要寒暄废话
- 但技术决策时需要详细分析，特别是 trade-off 说明

## 工作习惯
- 下午 2-4 点是深度工作时间，少打扰
- 每次部署前先问一下（不喜欢自动部署）
- 代码风格偏函数式，不喜欢 class

## 已知偏好
- 前端用 Vue 而非 React
- 部署平台首选 Vercel
- 编辑器是 VS Code
```

**看到区别了吗？** MEMORY.md 像是"办公室墙上贴的公司规定"——跟环境、工具、项目相关的客观事实。USER.md 像是"助理偷偷记下来的老板脾气"——你的习惯、偏好、风格。

**为什么分开？** 因为换一台电脑、换一个项目，MEMORY.md 的内容会变（新环境、新工具），但 USER.md 的内容基本不变（你还是你）。分开存，换环境时只要刷新 MEMORY.md，USER.md 可以跟着你走。

### 5.3 Agent 怎么读写记忆——具体操作过程

Agent 通过一个叫 `memory` 的工具来操作记忆，只有三个动作：

**动作一：add（加一条）**
```
Agent 觉得该记点什么了
    ↓
调用 memory(action="add", content="用户的项目用 pnpm 而非 npm")
    ↓
系统检查：
  1. 内容安全吗？（扫描12种注入攻击模式）
  2. 之前记过一模一样的吗？（重复就跳过）
  3. 加上这条会不会超 2200 字符上限？（超了就拒绝，告诉 Agent 当前用量）
    ↓
都没问题 → 写入文件
```

**动作二：replace（改一条）**
```
Agent 发现之前记的过时了
    ↓
调用 memory(action="replace", old_text="用户用 npm", content="用户改用 pnpm 了")
    ↓
系统在文件里找到包含"用户用 npm"的那条
    ↓
整条替换成"用户改用 pnpm 了"
```

**动作三：remove（删一条）**
```
Agent 发现某条记忆没用了
    ↓
调用 memory(action="remove", old_text="那条关于 npm 的")
    ↓
系统找到包含这句话的条目，删掉
```

**为什么用"文字片段"匹配而不是 ID？**

这是个很聪明的设计。你想想——LLM 天生擅长记住语义片段（"上次那个关于 pnpm 的"），但让它记住一个 UUID（a3f7b2c1）基本不可能。这个设计把操作方式适配给了 LLM 的认知特点。就像你跟同事说"改一下上次那个部署的笔记"，而不是说"请修改 ID 为 a3f7b2c1 的条目"。

### 5.4 多个会话同时写记忆怎么办——并发安全

想象一下：你同时在两个聊天窗口跟 Agent 说话，两个会话都想往 MEMORY.md 加内容。如果不管控，后写的会覆盖先写的。

Hermes 的写操作遵循一套严格的六步协议：

```
第①步：扫描内容——检查12种注入攻击模式（防止有人通过操纵记忆劫持 Agent）
    ↓
第②步：获取排他文件锁——别人这时候不能写
    ↓
第③步：在锁内重新从磁盘读取——确保拿到的是最新内容，不是缓存
    ↓
第④步：重复检测——一模一样的内容不加
    ↓
第⑤步：字符限制检查——超了不加
    ↓
第⑥步：原子写入——先写到临时文件，再原子性地替换原文件
```

**为什么第③步很重要？** 因为获取锁的时候，可能别人刚好写了一条进去。如果不重新读，你手里的就是旧数据，你一写就把别人刚加的覆盖了。

**为什么第⑥步用"临时文件 + 替换"？** 这是原子写入的经典做法。先写到临时文件，确认没问题了，再一步替换原文件。这样即使写到一半断电，也不会产生半个坏文件。

### 5.5 Agent 怎么知道"该记了"

Agent 不会天生知道该保存什么。这个"自觉"是 system prompt（系统提示词）里教它的：

```
什么时候该保存记忆（主动做，别等用户提醒）：
- 用户纠正你了，或者说了"记住这个""别再这样了"
- 用户分享了一个偏好、习惯或个人信息
- 你发现了关于环境的新东西（操作系统、工具、项目结构）

优先级：用户偏好和纠正 > 环境事实 > 操作经验
最有价值的记忆，是能防止用户再次纠正你的记忆。
```

最后这句话是精髓——**"最有价值的记忆是防止用户再次纠正你的记忆。"** 也就是说，Agent 优先记的是"用户纠正过你的事"（因为你被纠正了说明你之前做错了，记住了下次就不会再犯），然后再记环境事实，最后记操作经验。

**📝 代码讲解——这段 system prompt 在代码里长什么样**

这段关于"该存什么"的指令，实际上是在 `prompt_builder.py` 里拼进 system prompt 的。简化版代码大概是这样的：

```python
# agent/prompt_builder.py 中的记忆写入引导

MEMORY_GUIDANCE = """\
## 记忆写入规则

你应该主动管理你的记忆（MEMORY.md），不要等用户提醒你。

**什么时候该记：**
1. 用户纠正了你 → 立刻记住（最高优先级）
2. 用户说了"记住这个""不要再XXX了" → 立刻记住
3. 你发现了环境特性 → 适当记住（操作系统版本、工具路径等）

**优先级排序：**
用户纠正 > 用户偏好 > 环境事实 > 操作经验

**记忆写入工具：**
- memory(action="add", content="要记的内容")  # 新增
- memory(action="replace", old_text="旧内容", content="新内容")  # 修改
- memory(action="remove", old_text="要删的内容")  # 删除
"""

def build_system_prompt():
    # 把记忆引导指令拼到 system prompt 里
    prompt = ""
    prompt += load_hermes_soul()          # SOUL.md
    prompt += MEMORY_GUIDANCE             # ← 就是这里
    prompt += load_skills_index()         # 技能目录
    prompt += load_frozen_memory()        # MEMORY.md + USER.md 快照
    return prompt
```

你看，代码逻辑其实很简单——就是拼字符串。真正聪明的是**内容设计**——它不是在命令 Agent "你必须存"，而是**用优先级排序引导 Agent 做正确的事**。Agent 读到"用户纠正优先级最高"，就自然知道"哦，刚才用户吼我了，这个要先记下来"。

### 5.6 冻结快照——一个特别精妙的设计

这是 Hermes 记忆系统中最巧妙的工程细节，我给你掰开讲。

**先说问题是什么：**

大模型 API 有个优化叫**前缀缓存**。简单说就是：如果两次请求的前面部分一模一样（比如 system prompt 相同），第二次请求可以复用第一次的中间计算结果，大幅降低延迟和成本。但如果 system prompt 变了哪怕一个字，缓存就全废了。

Hermes 的记忆是注入到 system prompt 里的。如果会话中途 Agent 写了一条新记忆，system prompt 就变了，前缀缓存就废了——延迟和成本飙升。

**Hermes 的解决方案：冻结快照**

```
会话开始时：
    从磁盘读取 MEMORY.md 和 USER.md
    ↓
    生成一份"快照"，注入 system prompt
    ↓
    🔒 冻结！整个会话期间这个快照不再改变

会话中途 Agent 想加新记忆：
    写入磁盘 → 磁盘上的记忆文件立即更新
    ↓
    但 system prompt 里的快照不变（冻结了）
    ↓
    Agent 的工具返回值里包含最新内容（Agent 知道自己刚写了什么）
    ↓
    前缀缓存不受影响 ✅

下次新会话开始：
    重新从磁盘读取 → 包含上次写入的内容
```

**一句话总结这个设计**：**读写分离**——读（注入 system prompt）来自冻结快照，写（持久化）直接到磁盘。这样既保护了缓存效率，又不影响功能。在工程效率和功能完整性之间做了个很务实的权衡。

**📝 代码讲解——冻结快照是怎么实现的**

别怕，概念听起来高级，实现其实很简单。核心就一个原则：**在会话开始时把记忆内容复制一份，之后死活不动这份复制。**

```python
# run_agent.py 中启动对话时的简化逻辑

class AIAgent:
    def __init__(self):
        self.frozen_memory = None  # 冻结快照，整个会话期间不变

    def start_conversation(self):
        """每次新会话开始时调用"""

        # 第1步：从磁盘读取最新记忆
        fresh_memory = open("MEMORY.md").read()
        fresh_user = open("USER.md").read()

        # 第2步：生成快照（就是复制一份字符串）
        self.frozen_memory = fresh_memory + "\n" + fresh_user

        # 第3步：冻结！
        # 之后组 system prompt 只从 self.frozen_memory 取
        # 不重新读磁盘

    def build_system_prompt(self):
        """每次调用模型前组装 system prompt（一回合可能调多次）"""
        prompt = ""
        prompt += self.hermes_soul           # SOUL.md
        prompt += self.frozen_memory         # ← 永远用冻结版，不重读
        prompt += self.skills_index          # 技能目录
        return prompt
        # 因为 frozen_memory 不变 → system prompt 不变 → 前缀缓存命中 ✅

    def handle_add_memory(self, content):
        """Agent 中途要加一条记忆"""
        # 直接写磁盘（真实持久化）
        append_to_file("MEMORY.md", content)

        # 但 self.frozen_memory 不动！
        # Agent 能通过工具返回值知道自己刚写了什么
        # 只是 system prompt 里的快照不变 → 前缀缓存继续有效
```

**💡 生动类比**：你在饭店点菜，服务员把你的菜单记在一张小纸条上（冻结快照），每次进厨房拿菜都掏这张纸条看，不看你的原始菜单。就算你在桌上又加了一道菜（Agent 写新记忆），服务员的小纸条不变，他的行动路径不变（前缀缓存命中）。但新加的菜确实被写进了厨房的总记录（磁盘写入），下次你来时能看到。**这个设计的本质：在这次服务里，用小纸条换速度。**

### 5.7 聊天记录太多记不下怎么办——session_search

3,600 字符只能存最精华的事实。但你跟 Agent 可能聊了几万字的对话，这些细节存在哪？怎么找回来？

Hermes 用 `session_search` 工具来搜索**所有历史聊天记录**：

**两种模式：**

**模式一：浏览模式**（不传搜索词）
```
Agent 调用 session_search()（不传参数）
    ↓
返回最近的会话列表（标题、时间戳）
    ↓
零 LLM 成本，纯数据库查询
```

**模式二：搜索模式**（传搜索词）
```
Agent 调用 session_search(query="上周讨论的部署方案")
    ↓
SQLite FTS5 全文检索（就是数据库自带的全文搜索引擎）
    ↓
搜到 50 条匹配结果
    ↓
按会话分组，排除当前正在进行的对话
    ↓
以关键词位置为中心，截取前后各 50K 字符的窗口
    （不是简单截取开头，而是围绕你搜的词来截）
    ↓
并行调用小模型给每条结果生成摘要
    ↓
摘要失败也没关系，降级返回原文前 500 字符预览
```

**技术细节**：用的是 SQLite 的 FTS5 虚拟表，支持中文搜索，零外部依赖，查询速度毫秒级。你不需要装什么 Elasticsearch，一个 SQLite 文件搞定。

### 5.8 Honcho——Agent 怎么"理解"你

这是记忆系统中最"高大上"的一层，但默认不开。

**Honcho** 是 Plastic Labs 做的用户建模引擎。Hermes 可以选装它。核心原理借鉴了黑格尔的**"正-反-合"辩证法**：

```
正题：当前 Agent 对你的理解（比如"这个用户喜欢简洁的回答"）
    ↓
反题：新对话中出现矛盾或补充信息
    （比如你突然说"这次给我详细点，别太简略"）
    ↓
合题：用 LLM 把正题和反题融合，生成更新的用户理解
    （"这个用户平时喜欢简洁，但做技术决策时需要详细分析"）
    ↓
（循环——下次有新信息继续融合）
```

**四个工具：**
- `honcho_context`：获取当前用户上下文
- `honcho_profile`：查看/更新用户画像
- `honcho_search`：搜索历史交互
- `honcho_conclude`：总结得出结论

**为什么默认不开？** 因为它涉及隐私敏感数据（要发到外部服务）、有额外成本开销、短期使用场景收益有限。你需要的时候手动开。

### 5.9 五层记忆总结

| 层 | 类型 | 怎么存 | 存多久 |
|:---|:---|:---|:---|
| **L1** | 模型上下文窗口（短期推理） | 在模型的"脑子"里 | 会话内 |
| **L2** | SKILL.md 技能文件 | Markdown 文件 | 永久 |
| **L3** | 向量存储索引 | Embedding 向量 | 永久 |
| **L4** | Honcho 用户画像 | 正-反-合 LLM 融合 | 持续进化 |
| **L5** | 全文搜索 | SQLite FTS5 | 永久 |

### 5.10 实践落地——SOUL.md、USER.md、MEMORY.md 到底怎么写

前面讲了原理，下面给你**可以直接用的模板**。这三个文件是 Hermes 的"输入系统"，决定了它怎么工作、怎么理解你。

**📝 SOUL.md 模板——告诉 Agent "你是谁、怎么跟我协作"**

```markdown
# SOUL.md

## 立场
- 你可以主动提出想法，可以反对我的观点，但要给出理由
- 不是我的工具，是我的协作者
- 你的目标是帮我做出更好的决定，不是讨好我

## 自主性边界
以下事你可以自己做，不用问我：
  - 读写本地文件
  - 搜索公开信息
  - 生成报告、代码、文档
  - 安装开发工具（但要先告知）

以下事必须先问我：
  - 公开发布任何内容
  - 购买付费服务
  - 给真实的人发消息
  - 删除重要文件
  - 暴露私人信息

## 反对与纠偏
你可以反对我，但需要提供：
  - 数据或事实依据
  - 一个更好的替代方案
  - 清晰的推理过程

## 自我改进
当发现反复执行的操作流程时：
  - 主动沉淀为 Skill
  - 标注适用范围和前提条件
```

**💡 关键**：不要写"你是一个聪明、强大的 AI 助手"——这是废话。要写"你应该怎么和我协作"。

**📝 USER.md 模板——五个维度画像**

```markdown
# USER.md

## Profile（我是谁）
- 职业：后端开发 / 转 AI 方向
- 技术背景：Python 3年，正在学前端和 Agent 相关
- 当前关注的 3 个方向：AI Agent 架构、MCP 协议、RL 训练

## Communication（沟通偏好）
- 默认语言：中文
- 回答风格：简洁直接，不要寒暄废话
- 技术决策时需要详细分析和 trade-off 对比
- 讨厌：重复我已知的信息、回答太长却没有重点

## Output（输出偏好）
- 技术方案：要包含方案对比、选型理由、潜在风险
- 代码：优先给能直接跑的完整例子
- 写作：结构清晰，要点先行

## Collaboration（协作风格）
- 我经常切换想法，不要慌，跟着走就行
- 如果对话跑偏了，可以提醒我聚焦
- 更看重真实判断，不需要情绪安慰
```

**📝 MEMORY.md 模板——八个维度的工作笔记**

```markdown
# MEMORY.md

## Active Context（当前大方向）
- 学习 Hermes Agent 源码，准备 Agent 方向面试
- 不是：前端框架对比、数据库优化、DevOps

## Current Priorities（当前最重要的 3 件事）
1. 完整理解 Hermes 自进化闭环 → 能画出来 + 能讲出来
2. 准备 20 道 Agent 面试高频题
3. 搭建 Hermes 实践环境

## Active Projects
### 项目 A：Hermes Agent 学习
- 目标：深入理解源码和架构
- 进展：已完成 21 章学习笔记
- 下一步：实际部署 + 运行测试

## Decision Principles（决策原则）
- 遇到复杂问题 → 先拆成 3-5 个子问题
- 选型 → 先列对比表，再说推荐方案
- Agent 类任务 → 优先级：安全 > 正确 > 速度

## Known Pitfalls（踩过的坑）
- pnpm 在 monorepo 下需要加 -w 参数
- Vercel 部署 SSL 证书偶尔需要手动续期

## Environment（环境）
- macOS 15, Apple Silicon
- Node.js v22, Python 3.12
- VSCode, pnpm, Vercel CLI

## Memory Maintenance（维护规则）
- 只存长期稳定信息
- 过期的项目信息要清理
- 每周检查一次，删掉不再相关的条目
```

**💡 实用技巧——让 Hermes 帮你初始化记忆**

你可以直接告诉 Hermes 这段话，让它通过访谈帮你生成上述文件：

> "请帮我初始化 Memory。先通过访谈了解我（问关于用户画像、沟通偏好、当前项目、工作流、决策原则、工具环境的问题），然后自动生成 USER.md 和 MEMORY.md。不要写入 API Key、密码或隐私信息。"

**信息分层原则**：

| 信息类型 | 放哪里 | 生命周期 |
|:---|:---|:---|
| 稳定偏好、长期目标 | USER.md / MEMORY.md | 长期，定期更新 |
| 反复执行的流程 | Skill | 长期，按需调用 |
| 一次性任务过程 | 当前会话 | 会话结束即丢弃 |
| 大量资料、日志 | 文件系统 / 知识库 | 按需检索 |

**Memory 不是越完整越好——是越高频、稳定、能改变回答质量的越好。**

### 5.12 记忆解决了什么，还缺什么

**解决了**：Agent 能记住用户偏好、环境事实，能搜索历史对话细节。你不用每次重复指示。

**还没解决**：MEMORY.md 存的是**事实**——"项目用 pnpm"。但 Agent 花了 15 步才搞定的复杂部署流程，这个**操作步骤**塞不进 2,200 字符。下次遇到类似任务还得从头摸索。

→ **这就是技能系统要解决的问题**

### 5.13 项目级配置——AGENTS.md / CLAUDE.md（让 Agent 理解当前项目）

前面讲的 MEMORY.md 和 USER.md 是**全局的**——不管你在哪个项目里跟 Agent 说话，它都加载。但如果你有多个项目，每个项目的规则不同怎么办？

Hermes 支持项目级上下文文件，**只在进入该目录时才加载**：

```
~/project-a/
├── AGENTS.md       ← 项目 A 的规则，进这个目录就加载
├── CLAUDE.md       ← 兼容 Claude Code 的格式
└── .cursorrules    ← 兼容 Cursor 的格式

~/project-b/
└── AGENTS.md       ← 项目 B 的规则，互不干扰
```

**AGENTS.md 模板：**

```markdown
# AGENTS.md

## 项目目标
这是我的 Hermes Agent 实验目录，用于测试 Memory、Skill、MCP、Cron 和 Profiles 能力。

## 工作规则
- 所有报告输出为 Markdown
- 所有测试数据使用虚构数据
- 不读取公司文件
- 不上传密钥、真实聊天记录和隐私数据
- 安装工具前必须先生成风险说明
- 删除文件、安装依赖、发送外部消息都需要人工确认
```

**💡 与 MEMORY.md 的关键区别**：

| | MEMORY.md | AGENTS.md |
|:---|:---|:---|
| **加载范围** | 全局（每次对话都加载） | 仅当前目录 |
| **生命周期** | 跟你走 | 跟项目走 |
| **适合存什么** | 你的偏好、习惯、长期目标 | 项目规则、代码规范、目录结构 |

多项目切换时，把项目规则放到 AGENTS.md 更高效——不会让全局记忆被"项目 A 用 pnpm、项目 B 用 yarn"这种冲突信息塞爆。

**好输入的四个要素**（面试时可以说）：

```
背景 → 目标 → 约束 → 输出格式

❌ "帮我看看日志有什么问题"
✅ "app.log 最后 200 行中找出所有 ERROR（背景），
    按错误类型归类并给出修复建议（目标），
    只分析应用层错误，跳过基础设施警告（约束），
    输出为表格，包含错误类型、出现次数、示例、建议（格式）"
```

### 5.14 如果你跟别人解释

> "Hermes 的记忆就两个文件：MEMORY.md 记工作笔记（2200字上限），USER.md 记用户画像（1375字上限）。容量故意很小，逼它只记最重要的。读写分离——读的时候从冻结快照读（保护 API 缓存），写的时候直接写磁盘。聊天记录太多存不下？用 SQLite 全文搜索来翻历史。还有个可选的 Honcho 用户画像系统，用辩证法不断更新对你的理解。"

---
